get:
  description: download income sources as csv
  summary: download income sources as csv
  operationId: downloadIncomeSourcesAsCsv
  tags:
    - Income Source
  security:
    - bearerAuth: []
  responses:
    "200":
      description: income sources fetched successfully
      content:
        application/json:
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
                example: 200
              status:
                type: string
                example: success
              data:
                type: object
                properties:
                  path:
                    type: string
                    example: /income-sources
    "401":
      $ref: ../../components/responses/401.yaml
